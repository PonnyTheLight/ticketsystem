export const Config = {
    // Dont touch this version Setting.
    version: "0.0.0",

    default_languaje: "en",
    db_schema: "test",
    modules_enabled: {
        general_settings: true,
        blog: false,
    }
}